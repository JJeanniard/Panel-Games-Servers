{% extends 'base.html.twig' %}

{% block title %}Ip{% endblock %}

{% block body %}
    <h1>Ip</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ip</th>
                <td>{{ ip.ip }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ip.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ip_index') }}">back to list</a>

    <a href="{{ path('ip_edit', {'ip': ip.ip}) }}">edit</a>

    {{ include('ip/_delete_form.html.twig') }}
{% endblock %}
