{% extends 'base.html.twig' %}

{% block title %}Dedier{% endblock %}

{% block body %}
    <h1>Dedier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ dedier.name }}</td>
            </tr>
            <tr>
                <th>SshLogin</th>
                <td>{{ dedier.sshLogin }}</td>
            </tr>
            <tr>
                <th>SshPassword</th>
                <td>{{ dedier.sshPassword }}</td>
            </tr>
            <tr>
                <th>SshKey</th>
                <td>{{ dedier.sshKey }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ dedier.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dedier_index') }}">back to list</a>

    <a href="{{ path('dedier_edit', {'name': dedier.name}) }}">edit</a>

    {{ include('dedier/_delete_form.html.twig') }}
{% endblock %}
